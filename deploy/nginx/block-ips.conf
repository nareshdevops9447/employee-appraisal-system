# block-ips.conf — Manually managed IP blocklist
# Add offending IPs here in the format:
#   deny <IP>;
#
# Examples:
#   deny 1.2.3.4;
#   deny 10.0.0.0/8;
#
# After editing, reload Nginx:
#   docker exec eas-nginx nginx -s reload
# -------------------------------------------------
